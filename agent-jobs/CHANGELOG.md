# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

### Mac app (Swift rewrite, in progress)

- **M07 — Visual Identity** (2026-04-27): Custom service-tray menu-bar glyph (asset-catalog `MenuBarIcon.imageset` + new `IdentityImage.loadMenuBarNSImage()`; template-flagged; SVG source of truth under `Resources/Identity/`); 1024 system app icon shipped at 5 sizes @1x/@2x via `AppIcon.appiconset` regenerated by `scripts/build-icns.sh`. Count-badge variant (`BadgeText` 3-branch contract: hidden for 0, digit for 1..9, "9+" for ≥10). New `DesignTokens.SemanticColor` (running/scheduled/failed/idle) + `SourceColor` (5-bucket palette) + `Typography.{display,title,body,caption,mono}` + `Spacing.{xs,sm,md,lg,xl}=4/8/12/16/24` namespaces, additive (legacy `StatusColor` typealiased so M02–M06 baselines stay byte-stable); `SemanticColor` adopted across StatusBadge, MenuBarRowViews, SourceBucketChip, MenuBarLabel. T-019 `nameColumnMinWidth = 210` (≥30% of 700pt list pane); T-020 sidebar "Filters" header band heightened to 40pt (architect option (b)). Capture-all renumbered 10 → 14 scenarios (4 menubar-icon variants + 1 tokens-swatch). Five M06 watch-list items folded in: WL-A `Snapshot.forceAppearance` → `forceDarkAppearance` + dark-only precondition; WL-B PNG/sidecar byte-stable short-circuit; WL-C dead helpers + `ServiceRowCompact.swift` purged with static-grep `DeadCodeTests`; WL-D canonical sidecar-schema sentence appended to `.workflow/DESIGN.md`; WL-E flaky wallpaper-sampling test replaced by deterministic asset-catalog + offscreen-render check. Cycle-1 tester REJECT 24/26 on AC-V-01 (placeholder-glyph central-8×8 luma 0.631) + AC-V-04 (dark glyph captured fully transparent — SwiftUI offscreen drops template-flagged Image silently); cycle-2 implementer closed both via real service-tray glyph (central luma 0.000 light, 0.968 dark) + private `MenuBarIconOnlyView` with SwiftUI `Canvas` mirror in dark branch. T-001/T-019/T-020/T-T01/T-T02/T-T03/WL-A..E closed. 18/18 testable functional ACs PASS; ui-critic ENFORCING **PASS 25/30** (threshold 24/30; cycle-1 ui-critic skipped per protocol after tester REJECT). Two new P2 polish tickets filed (T-021 strip placement in empty dashboard, T-022 baseline alignment after option (b)) — both deferred to M14. (+26 tests, 332 → 358)
- **M06 — Information Architecture** (2026-04-27): First milestone under ENFORCING ui-critic gate. Popover redesigned to ≥480pt wide with grouped rich rows (status pill + friendly title + 1-line summary; RUNNING/SCHEDULED/FAILED/OTHER priority order); FAILED rows expose inline `RetryAffordance` button. Dashboard launches at 1280×800 default with sidebar 220 / inspector 360 / list min 480 via `DashboardWindowConfig` + `.navigationSplitViewColumnWidth` modifiers. `SourceBucketStrip` rebuilt as horizontal pill chips matching popover treatment with "total N" inline; 0-count chips dimmed (opacity 0.55) + tooltip via `bucket.emptyExplanation`. `Snapshot.capture` rewritten to host `NSHostingView` in an offscreen ordered-front `NSWindow` with appearance pin + recursive `forceAppearance` walk + layer invalidation, all dark-only-gated to keep M02/M03/M04 light baselines byte-stable; dashboard `Table` rows + dark scheme now render in capture-all output. Empty popover renders RUNNING(0)/SCHEDULED(0)/FAILED(0) scaffolding with per-section microcopy via `PopoverGrouping.groupByStatus(includeEmpty: true)`. `AgentJobsMacUI.swift` pre-emptively split (504 LOC < 600); `ProviderDiagnostics` actor demoted to `internal` via new `DiagnosticsBearing` protocol. T-002/T-003/T-008/T-014/T-015/T-016/T-017/T-018 closed. 19/19 testable ACs PASS; ui-critic ENFORCING PASS-with-tickets 27/30 (cycle-1 REJECT 20/30 cleared after dark-frame fixes). (+15 tests, 317 → 332)
- **M05 — Content fidelity + Visual Harness library** (2026-04-24): New `AgentJobsVisualHarness` SwiftPM library (`Snapshot`, `MenuBarInteraction`, `WindowInteraction`, `CritiqueReport`, `DiffReport`) plus `capture-all` executable producing 10 PNG+JSON pairs in <2s for ui-critic gating. New `ServiceFormatter` (friendly title + 1-line summary) wired across popover row, dashboard row, inspector header. New `LiveResourceSampler` actor populating CPU%/RSS via `proc_pid_taskinfo` off the main thread. Cron T-004 root-cause fix: `ClaudeSessionCronProvider` no longer silently swallows per-file parse failures — `ProviderDiagnostics` actor surfaces `lastError` + per-file failure map; chip-tooltip plumbing added. Package surgery: `AgentJobsMac` executable renamed to `AgentJobsMacApp` (thin `@main`); new `AgentJobsMacUI` library extracted; 5-target package compiles cleanly. 24/24 ACs PASS.
- **M04 — Auto-refresh + fs.watch** (2026-04-24): `RefreshScheduler` actor with debounce + in-flight coalescing, DispatchSource-based `FileObjectWatcher`/`DirectoryEventWatcher` with atomic-rename re-open, `VisibilityProvider` for battery-saving pause, `PeriodicTicker` with keepalive, AutoRefreshIndicator 3-state (idle/refreshing/error) in both popover and dashboard toolbar; `WatchPaths` test seam + static-grep enforcement. 29/30 ACs PASS first-try. (+42 tests, 226 → 266)
- **M03 — Actions (stop/hide/refresh)** (2026-04-24): Row-hover + inspector stop buttons backed by `StopExecutor` (SIGTERM / `launchctl unload`); 6-predicate refusal (PID 0/1/self, missing PID/plist, unsupported sources) with defense-in-depth; `HiddenStore` actor with atomic persistence; manual refresh + spinner; destructive-action confirmation dialog; 7 visual baselines. 26/26 ACs PASS first-try. (+48 tests, 178 → 226)
- **M02 — Functional UI baseline** (2026-04-24): Dashboard shows all 5 discovery sources with `SourceBucketStrip` summary header (click-to-filter), inline `ServiceInspector` right pane, `.accessory` activation policy (menu bar icon as primary entry), visual-test harness with 6 committed screenshot baselines, app-launch smoke test. 26/26 ACs PASS. (+33 tests, 145 → 178)
- **M01.5 — Claude session-JSONL cron parser** (2026-04-24): Streams `~/.claude/projects/**/*.jsonl`, reconstructs CronCreate/CronDelete net set, dedups against `scheduled_tasks.json`. 15/15 ACs PASS. (+34 tests)
- **M01 — Discovery audit + gap fill** (2026-04-24): Adds `LsofProcessProvider` + `ClaudeScheduledTasksProvider`, wires both into the default registry, fixes launchd `createdAt` provenance from plist mtime. 37/37 ACs PASS. (+56 tests, 55 → 111)

## 0.1.0 (2026-04-11)

Initial public release.

### Features

- **TUI Dashboard**: Interactive terminal UI built with Ink (React for CLI)
  - Tab-based navigation (Dashboard, Settings)
  - Keyboard shortcuts: arrow keys, Enter to expand, q to quit
  - Inline detail panel with job metadata
- **Auto-Detection**: PostToolUse hook captures 14 service patterns
  - pm2, nohup, docker run, systemctl, launchctl, docker-compose
  - flask, uvicorn, gunicorn, next dev, vite dev, and more
- **Live Process Scanner**: Discovers running services via lsof
  - Port extraction, friendly names, framework detection
- **Cron Task Scanner**: Reads Claude Code scheduled tasks
- **Agent Support**: Claude Code, Cursor, GitHub Copilot, OpenClaw
- **Human-Readable Display**: cronToHuman schedules, relative time AGE column
- **CLI Commands**: `agent-jobs` (TUI), `agent-jobs list` (JSON), `agent-jobs setup`/`teardown`
- **Zero-Config Install**: postinstall hook auto-registers the PostToolUse hook
- **Atomic Writes**: temp-file-then-rename for jobs.json and settings.json
