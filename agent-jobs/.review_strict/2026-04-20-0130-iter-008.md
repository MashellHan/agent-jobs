# Iteration 008 — Strict Review

**Time:** 2026-04-20 01:30 (cron-triggered)
**Reviewer:** Claude (strict review loop)
**Target:** `agent-jobs/macapp/AgentJobsMac/`
**Previous iteration:** 007 (PASS-with-WARN — H-004 commit drift, M-007 false-positive)
**Last reviewed HEAD:** `e53dbd6` + dirty tree
**Current HEAD:** `c67de9c` (+2 commits, cycle-9 + cycle-10)

---

## Summary

**Verdict: ✅ PASS — H-004 / M-007 both resolved cleanly. 34/34 tests, 2 honest commits.**

The implementer addressed iter-007's two top concerns in a tight 2-cycle sprint:
- **cycle-9 (`297aafb`)** committed the leaked LaunchdUserProvider work as a single coherent change (resolves H-004)
- **cycle-10 (`c67de9c`)** introduced `discoverAllDetailed() → DiscoverResult { allFailed }` and rewrote `refresh()` to only flip to `.error` when `allFailed` (resolves M-007). Same cycle extracted 8 atoms from `MenuBarViews.swift`, dropping it from 324→120 LOC.

Build green, 34/34 tests, no commit-message lies caught.

---

## Progress vs Iter-007

| Iter-007 issue | Status |
|----------------|--------|
| **H-004** Feature work uncommitted in working tree | ✅ **RESOLVED** (cycle-9 committed it) |
| **M-005** Commit-gate hook not wired | ⚠️ **OPEN** — hook still not added; this round's correct behavior was again voluntary |
| **M-006** `LaunchdUserProvider.createdAt = Date()` synthesized | ⚠️ **OPEN** — `LaunchdUserProvider.swift:86` unchanged |
| **M-007** `LoadPhase.error` false-positive on legit-empty | ✅ **RESOLVED** (`discoverAllDetailed.allFailed`) |
| L-005 Shell.onCancel DispatchQueue | OPEN (nit) |
| L-006 readWithTimeout sync IO | OPEN (acceptable) |
| L-007/L-008/L-009 Launchd schedule/kind/command | OPEN (defer) |

**Net:** 2 of 4 MEDIUM-or-higher resolved this cycle. M-005 still the chronic process gap.

---

## Build & Test

```
$ swift build
Build complete! (1.78s)

$ swift test
Test run with 34 tests passed after 0.304 seconds.
```

| Check | Result | Time |
|-------|--------|------|
| `swift build` | ✅ | 1.78s |
| `swift test` | ✅ | 0.30s, **34/34** |

⚠️ Test count unchanged at 34 despite cycle-10 introducing the new `DiscoverResult` API. **No tests added** for `discoverAllDetailed.allFailed` — see M-008 below. The existing `failing provider does NOT poison the rest` test exercises the failure-isolation path but not the per-provider counting.

---

## Code Review

### `ServiceRegistry.swift` (+24 LOC, now 72 lines)

**✅ Strengths**:
- `DiscoverResult { services, succeededCount, totalCount, allFailed }` is a clean value type, `Sendable` correct.
- `allFailed` derivation is correct: `totalCount > 0 && succeededCount == 0` — guards against the "no providers configured" case being mis-classified as failure.
- `discoverAll()` retained as a one-line wrapper around the detailed path → no breaking change for existing callers.
- `withTaskGroup` now collects `(slice, ok)` tuples — minimal change, still failure-isolated, still concurrent.
- Docstrings reference M-007 explicitly. Future archaeologists thank you.

**⚠️ Findings**:

| ID | Severity | Issue |
|----|----------|-------|
| M-008 | MEDIUM | New `DiscoverResult.allFailed` logic has **zero test coverage**. The bug fix is untested — a future refactor that flips `>` vs `>=` or inverts the boolean has no safety net. Add 3 quick tests: (1) all providers fail → `allFailed == true`, (2) one succeeds → `allFailed == false`, (3) all succeed but return `[]` → `allFailed == false`. |

### `AgentJobsMacApp.refresh()` diff

```swift
let result = await registry.discoverAllDetailed()
services = result.services.sorted { ... }
...
if result.allFailed {
    phase = .error("All providers failed to respond")
} else {
    phase = .loaded
}
```

✅ Correct logic. The error message `"All providers failed to respond"` is honest now (not the misleading `"No providers responded"` from iter-007). One nit: `result.allFailed` will be `false` when `totalCount == 0` (no providers configured) — but that's by design and matches iter-007's stated requirement.

### Components extraction (`Components/*.swift`)

8 new atoms, all between **17–70 LOC**:

| File | LOC | Notes |
|------|-----|-------|
| MenuBarLabel | 26 | Menubar entry-point widget |
| SummaryChip | 21 | Pill of "N running" etc. |
| MemoryBadge | 17 | Memory cap indicator |
| HoverableIconButton | 25 | Reusable hover affordance |
| ErrorBanner | 33 | Refresh-failed strip + Retry button |
| ServiceRowCompact | 70 | Largest atom, still focused |
| EmptyHintView | 17 | "No services" empty state |
| SkeletonRow | 30 | Loading shimmer placeholder |

All ≤70 LOC, single-responsibility, accessibility labels in place where applicable. `ErrorBanner` has `.accessibilityElement(children: .combine)` + composed label — a11y solid.

`MenuBarViews.swift` shrunk **324 → 120 LOC (-63%)**. Excellent.

### File-size watch

| File | LOC | Δ | Ceiling |
|------|-----|---|---------|
| `MenuBarViews.swift` | 120 | -204 | 400 |
| `DashboardView.swift` | 272 | 0 | 400 |
| `ServiceRegistry.swift` | 72 | +24 | 400 |
| `AgentJobsMacApp.swift` | ~128 | +6 | 400 |
| `LaunchdUserProvider.swift` | 95 | 0 | 400 |
| 10× Components/*.swift | 17-70 | NEW | 400 |

All within budget. 10 atoms in `Components/` is a healthy population.

---

## MEMORY-preference cross-check

| Preference | Status | Notes |
|------------|--------|-------|
| feedback_tui_design (HIG, inline detail) | ✅ | 8 new atoms continue Components/ extraction; `MenuBarViews` is now a layout file, not a god view |
| feedback_tui_history (created_at, friendly history) | ⚠️ | M-006 still open — launchd `createdAt` synthesized |
| feedback_schedule_display (real frequency) | ⚠️ | L-007 still open — launchd `.onDemand` not honest |
| feedback_openclaw (additional source) | ⏭ | 2 of "4 sources" goal (agent-jobs + launchd); cron + openclaw not yet |
| feedback_autonomous | ✅ | implementer ran build/test before commit |
| feedback_documentation | ✅ | Docstrings on `DiscoverResult` reference M-007; CHANGELOG updated |
| feedback_auto_commit_push | ✅ | **Recovered** — cycle-9 captured the H-004 leak as a single coherent commit |
| project_agent_collaboration | ✅ | impl docs at `.implementation/impl-2026-04-20T*.md`; cycle-10 explicitly references iter-007 M-007 finding |

**`feedback_auto_commit_push` recovered** — but that's because the implementer remembered, not because the harness enforced (M-005 still open).

---

## Findings

### CRITICAL
*(none — build green)*

### HIGH
*(none — H-004 resolved)*

### MEDIUM

| ID | Title | Where | Action |
|----|-------|-------|--------|
| M-005 | Commit-gate hook not wired (carried from iter-006) | `.claude/settings.json` PreToolUse | Add `swift build && swift test` PreToolUse for `Bash(git commit:*)`. Two cycles in a row "saved by implementer discipline" — luck doesn't compound. |
| M-006 | `LaunchdUserProvider.createdAt = Date()` synthesized — violates feedback_tui_history | `LaunchdUserProvider.swift:86` | Make `Service.createdAt` optional or add a `provenance` flag. UI must not display synthesized time as authoritative. |
| M-008 | NEW: `DiscoverResult.allFailed` logic untested | `ServiceRegistryTests.swift` | Add 3 tests: all-fail, partial-success, all-empty-success. Without these, the M-007 fix has no safety net. |

### LOW (carried)

| ID | Title |
|----|-------|
| L-005 | `Shell.onCancel` DispatchQueue (nit) |
| L-006 | `readWithTimeout` sync IO (acceptable) |
| L-007 | Launchd `.onDemand` not honest (defer) |
| L-008 | Launchd `kind` is approximation (defer) |
| L-009 | Launchd `command: ""` should be nil (nit) |

---

## Decision

**✅ PASS** — first PASS without WARN since iter-006. Two consecutive PASS now achieved (006 + 008, with 007 being PASS-with-WARN that resolved its own findings in next cycle). 

**Termination tracker:**
- iter-006: PASS (1 PASS streak)
- iter-007: PASS-with-WARN (streak broken)
- iter-008: PASS (new streak: 1)

Per the original termination criterion ("2 consecutive clean PASS"), we still need iter-009 to also be PASS to terminate or downgrade frequency. The right call for next iteration:

1. Verify M-008 tests added (smallest, highest-leverage open item)
2. Re-evaluate M-005 — if no hook movement after 3+ iterations, propose I implement the hook directly rather than continuing to file the same finding
3. Re-evaluate M-006 — schema change, low effort

---

_End of iteration 008. Two clean cycles in a row from the implementer; the only thing standing between us and termination is the structural hook that's been carried since iter-005. The pattern is clear: ship the hook, finish the work._
