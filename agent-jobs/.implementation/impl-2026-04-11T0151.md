# Implementation Log — 2026-04-11 01:51

## Source: .test/report-2026-04-11T0133.md

## Changes Made

### 1. Column alignment fix (job-table.tsx)

- Moved indicator (`▶`/`▼`/` `) into the column system as `COL.indicator: 2`
- Added matching `<Box width={COL.indicator}>` in `TableHeader` for alignment
- Reduced column widths to fit 100-col ink test render: status=3, name=20, agent=12, schedule=12, source=10, lastRun=16, result=7
- Total: 82 cols + 14 gaps (7*2) = 96, safely under 100

### 2. Test isolation fix (detect.test.ts)

- Changed all `describe` blocks from `vi.clearAllMocks()` to `vi.resetAllMocks()` in `beforeEach`
- Re-apply default mock implementations (`existsSync`, `readFileSync`) after reset
- Prevents cross-test contamination from `mockReturnValue` overrides

### 3. New detect pattern tests (+6 tests)

- `docker run -d --name my-app nginx:latest` extracts `"my-app"` from `--name` flag
- `uvicorn main:app` produces `"uvicorn main:app"`
- `gunicorn app:application` produces `"gunicorn app:application"`
- `next dev` produces `"next-dev"`
- `vite dev` produces `"vite-dev"`
- All verify both detection AND correct name extraction via writeFileSync mock

### 4. Scanner friendlyLiveName unit tests (+12 tests)

- Exported `friendlyLiveName` from scanner.ts
- New file: `scanner.test.ts` with 12 tests covering:
  - Script extraction with/without port
  - Framework detection (next, vite, uvicorn, gunicorn, flask, nuxt)
  - Fallback to command + port
  - Script priority over framework detection

## Test Results

- Before: 51 tests (3 files)
- After: 68 tests (4 files), all passing
- New test file: scanner.test.ts

## Review/Test Conflicts

No conflicts. Test report recommendations were complementary to prior review items.
All changes align with both review and test guidance.
