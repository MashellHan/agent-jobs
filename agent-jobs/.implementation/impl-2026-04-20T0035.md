# Implementation Cycle 9 — 2026-04-20T00:35

**Module focus:** Discovery (cycle 9 mod 3 == 0)
**Time budget used:** ~12 / 30 min
**Reviews ingested this cycle:**
- `.review/code/code-003.md` ✅ (92/100, first ≥90 round) — addressing all 3 P1s
- `.review_strict/2026-04-20-0030-iter-006.md` ✅ (PASS, M-005 commit-gate noted)
- `.review_strict/_open_issues.md` ✅ (M-005 OPEN, L-005/L-006 OPEN-acceptable)
- `.design-review/design-002.md` (cycle-7 already shipped all P0 fixes)

## P1 fixes shipped

| ID | Stream | Fix |
|----|--------|-----|
| code-003 P1 #1 (M1) | code-review | `ServiceRegistryViewModel.refresh()` now sets `phase = .error("No providers responded")` when `discovered.isEmpty && providerCount > 0`. Cycle-7 ErrorBanner is now reachable. Added `ServiceRegistry.providerCount` actor-isolated accessor. |
| code-003 P1 #3 | code-review | `LaunchdUserProvider` shipped — parses `launchctl list` via `Shell.run("/bin/launchctl", ["list"])`, skips header + `com.apple.*` system jobs, maps PID/exit-status → ServiceStatus. Wired into `defaultRegistry()` as second production provider. |
| code-003 P1 #2 | code-review | **Deferred to cycle 10** (UI module per rotation). MenuBarViews.swift extraction is pure refactor and fits the cycle-10 (mod 3 == 1) UI focus. |

## Files touched (5)

- NEW `Sources/AgentJobsCore/Discovery/Providers/LaunchdUserProvider.swift` (~95 LOC)
- NEW `Tests/AgentJobsCoreTests/LaunchdUserProviderTests.swift` (~70 LOC, 7 cases)
- `Sources/AgentJobsCore/Discovery/ServiceRegistry.swift` — added `providerCount`, second provider in defaultRegistry()
- `Sources/AgentJobsMac/AgentJobsMacApp.swift` — `LoadPhase.error` wired in `refresh()`
- `CHANGELOG.md` — cycle 9 entry

## Test status

```
swift test → 34/34 passing in 0.317s  (was 27 last cycle, +7)
- AgentJobsJsonProvider:    6
- CronHumanizer:            8
- MenuBarSummary:           3
- Shell:                    5
- ServiceRegistry:          5
- LaunchdUserProvider:      7   ← NEW
```

Coverage trajectory: cycle 2 = 6 → 5 = 22 → 6 = 25 → 7 = 25 → 8 = 27 → 9 = 34 (+467% vs cycle 2).

## Strict-review CRITICAL/HIGH still open
**None.** M-005 (commit-gate hook) is process-layer, requires `.claude/settings.json` PreToolUse hook. Acknowledged but out-of-scope for code module — `swift build && swift test` was run before this commit, satisfying intent if not letter.

## Termination check progress

- Strict review iter-006 → ✅ PASS (3rd consecutive after iter-003, iter-004; iter-005 was BLOCK)
- code-review-003 → 92/100 (CONTINUE — needs code-004 ≥ 90 to be 2nd consecutive)
- design-review-002 → 82/100 (CONTINUE — needs design-003 ≥ 90)
- `swift test` green? → ✅ 34/34
- `_open_issues.md` empty? → ⚠️ M-005 OPEN (process), L-005/L-006 OPEN-acceptable

→ Cannot write `COMPLETE.md` yet. Awaiting code-004, design-003, and iter-007.

## Conflicts / Communication

- **Push status:** Cycle-8 commit `d5764a5` and code-003 commit `e53dbd6` are both **local-only** (push 403; remote `MashellHan/agent-jobs` rejects current credential `menha_microsoft`). User intervention needed (`gh auth login` or PAT rotation in keychain). All work tracked locally; squash-merge once credentials restored.
- **MenuBarViews extraction (code-003 P1 #2):** Deferred 1 cycle to align with module rotation (cycle 10 == UI). Not a regression — pure refactor, no behavior change blocked.

## Notes for next cycle (cycle 10, mod 3 == 1 → UI)

- Extract from `MenuBarViews.swift` (324 LOC) → `Components/`:
  - `SkeletonRow.swift`, `EmptyHintView.swift`, `SummaryChip.swift`,
    `MemoryBadge.swift`, `HoverableIconButton.swift`, `ErrorBanner.swift`,
    `ServiceRowCompact.swift`, `MenuBarLabel.swift`
- After extraction MenuBarViews.swift should hold only `MenuBarPopoverView`.
- Add `ServiceRegistryViewModelTests` covering LoadPhase state machine
  (idle→loading→loaded; loading→error; error→loading→loaded retry) — code-003
  Communication block recommendation. Locks in the cycle-9 contract.
- Consider also addressing code-003 L1 (StatusBadge symbol+color presentation
  tuple) as cosmetic cleanup if budget allows.
