# Implementation Cycle 12 — 2026-04-20T12:17

**Module focus:** Discovery layer (cycle 12 mod 3 == 0)
**Time budget used:** ~22 / 30 min
**Reviews ingested this cycle:**
- `.review_strict/2026-04-20-1201-iter-012.md` ✅ — PASS (streak: 2). MEDIUM table empty. Recommendation called out L-007/L-008 (parse plist for real schedule) as next implementer target.
- `.review_strict/_open_issues.md` ✅ — only LOWs remain.
- `.review/code/code-003.md`, `.design-review/design-002.md` — last consumed in earlier cycles, no newer files.

## L-fixes shipped (this is a "no MEDIUMs left" cleanup cycle)

| ID | Stream | Fix |
|----|--------|-----|
| strict L-007 | strict-review | New `LaunchdPlistReader` reads `~/Library/LaunchAgents/<label>.plist` (and `/Library/LaunchAgents`) and decodes `StartInterval` → `.interval(seconds:)`, `StartCalendarInterval` (single dict OR array) → `.calendar(components:)`. Tolerant of missing/malformed plists (`.empty`, never throws). Wired into `LaunchdUserProvider.parse(_:enrichWith:)` so launchd jobs surface their **real** schedule instead of placeholder `.onDemand`. Honors `feedback_schedule_display`. |
| strict L-008 | strict-review | `kind` is now `.scheduled` whenever the plist defines a trigger (StartInterval / StartCalendarInterval / WatchPaths / KeepAlive), not just when PID is nil. Same commit as L-007 — single root-cause fix. |
| design polish | feedback_schedule_display | `Schedule.humanDescription` for `.calendar` cases now renders meaningfully: `"daily at 09:00"`, `"weekly Mon at 03:30"`, `"monthly on day 15 at 12:00"`, `"hourly at :05"`, `"2× 09:00"` (multi same-time), or `"N calendar triggers"` (heterogeneous fallback). Replaces the previous static `"calendar trigger"` placeholder. launchd weekday (Sun=0) translated to Cocoa convention (Sun=1) on parse. |

## Files touched (5)

- NEW `Sources/AgentJobsCore/Discovery/Providers/LaunchdPlistReader.swift` (~165 LOC)
- NEW `Tests/AgentJobsCoreTests/LaunchdPlistReaderTests.swift` (~220 LOC, 17 tests)
- `Sources/AgentJobsCore/Discovery/Providers/LaunchdUserProvider.swift` — `init(runner:plistReader:)` + `parse(_:enrichWith:)` enrichment
- `Sources/AgentJobsCore/Domain/ServiceSource.swift` — `Schedule.humanizeCalendar(_:)` + `describe(_:)` + `weekdayShort(_:)`
- `macapp/AgentJobsMac/CHANGELOG.md` — cycle 12 entry

## Test status

```
swift test → 55/55 passing in 0.323s  (was 38, +17 from new LaunchdPlistReaderTests + ScheduleCalendarHumanizationTests)
swift build → green
```

## File-size watch

| File | LOC | Δ vs cycle 11 | Ceiling |
|------|-----|---------------|---------|
| `LaunchdPlistReader.swift` | 165 | new | 400 |
| `LaunchdPlistReaderTests.swift` | 220 | new | 600 |
| `ServiceSource.swift` | 152 | +58 (calendar humanizer) | 400 |
| `LaunchdUserProvider.swift` | 110 | +14 | 400 |

## Termination check progress

- Strict review iter-012 → PASS (streak: 2)
- M-* table → empty
- This cycle clears L-007 and L-008. Only L-005 / L-006 / L-009 remain (all marked nit/acceptable).
- `swift test` green? → ✅ 55/55
- One more clean strict cycle would qualify for `COMPLETE.md` per iter-012's recommendation.

## Conflicts / Communication

- **Push status:** Cycles 7..11 commits all **local-only** (5 ahead of `origin/main`). cycle-12 will be the 6th. Awaiting credential rotation (`menha_microsoft` 403 vs `MashellHan/agent-jobs`). Per `feedback_auto_commit_push` this needs human attention; auto-commit half is healthy.
- **Pre-commit gate:** Will fire again on this commit (5 of 5 staged files match `^macapp/AgentJobsMac/`). Expected swift build + test ≈ 2.3s overhead, both green.

## Notes for next cycle (cycle 13, mod 3 == 1 → UI)

- L-009 (`command: ""` cosmetic) — could promote `Service.command` to `String?` and let `LaunchdUserProvider` pass `enrichment.command` directly without `??""`. Touches Domain again. ~10-min job.
- Surface real `command` in the dashboard inspector "Overview" tab (already in the field, may need empty-state UX polish for plist-less rows).
- Consider sidebar filter "scheduled vs daemon" now that the kind distinction is honest.
