# Implementation Cycle 10 — 2026-04-20T00:50

**Module focus:** UI (cycle 10 mod 3 == 1)
**Time budget used:** ~14 / 30 min
**Reviews ingested this cycle:**
- `.review/code/code-003.md` ✅ (92/100; addressing P1 #2 MenuBarViews extraction)
- `.review_strict/2026-04-20-0100-iter-007.md` ✅ (PASS-with-WARN; H-004 already-resolved-by-cycle-9-commit, M-006/M-007/L-007/L-008/L-009 NEW)
- `.review_strict/_open_issues.md` ✅ (M-005 commit-gate STILL open at process layer)

## P1 + P0 fixes shipped

| ID | Stream | Fix |
|----|--------|-----|
| code-003 P1 #2 | code-review | MenuBarViews.swift split: 8 atoms extracted to `Components/` (MenuBarLabel, SummaryChip, MemoryBadge, HoverableIconButton, ErrorBanner, ServiceRowCompact, EmptyHintView, SkeletonRow). MenuBarViews.swift: **324 → 120 LOC** (−204), now holds only `MenuBarPopoverView`. |
| strict-iter-007 M-007 | strict-review | False-positive ErrorBanner on legit-empty registry. Added `ServiceRegistry.discoverAllDetailed()` returning `DiscoverResult` with per-provider success counts; view model now sets `.error` only when `result.allFailed` (every provider threw), not when they returned legitimate empty arrays. |
| strict-iter-007 H-004 | strict-review | Already addressed: cycle-9 work committed as `297aafb` before iter-007 ran (race condition in review). |

**Deferred (intentional):**
- M-006 (synthetic createdAt for launchd): `Service.createdAt` is non-optional `Date` per Domain contract. Making it optional is a cross-module refactor that touches every provider + the dashboard table — out of scope for cycle-10 UI. Documenting trade-off; will surface as a "discovered at" UI note rather than a domain change.
- L-007/L-008 (launchd schedule parsing from plist): proper `.calendar`/`.interval` mapping requires reading per-job plist files from `~/Library/LaunchAgents/`. Sized as its own cycle.
- L-009 (`command: ""` instead of nil sentinel): `Service.command` is non-optional by domain contract (cycle-2 H3 fix). Cosmetic.

## Files touched (12)

- NEW Components: `MenuBarLabel.swift` (26), `SummaryChip.swift` (21),
  `MemoryBadge.swift` (17), `HoverableIconButton.swift` (25),
  `ErrorBanner.swift` (33), `ServiceRowCompact.swift` (70),
  `EmptyHintView.swift` (17), `SkeletonRow.swift` (30)
- `Sources/AgentJobsMac/Features/MenuBar/MenuBarViews.swift` — 324→120 LOC
- `Sources/AgentJobsCore/Discovery/ServiceRegistry.swift` — added `DiscoverResult` + `discoverAllDetailed()`
- `Sources/AgentJobsMac/AgentJobsMacApp.swift` — `refresh()` uses detailed result; only `.error` when all providers failed
- `CHANGELOG.md` — cycle 10 entry

## Test status

```
swift test → 34/34 passing in 0.313s  (no regression — extraction is pure refactor)
```

## File-size watch (post-extraction)

| File | LOC | Δ vs cycle 9 | Ceiling |
|------|-----|--------------|---------|
| `MenuBarViews.swift` | 120 | −204 | 400 |
| `DashboardView.swift` | 272 | 0 | 400 |
| `ServiceRegistry.swift` | 71 | +33 | 400 |
| `Components/*` (largest) | 70 | new | 400 |

## Termination check progress

- Strict review iter-007 → PASS-with-WARN (M-005 still open at process layer; new M-006/M-007 raised; M-007 fixed this cycle)
- code-review-003 → 92/100 (CONTINUE — needs code-004 ≥ 90 to be 2nd consecutive)
- design-review-002 → 82/100 (CONTINUE — needs design-003 ≥ 90)
- `swift test` green? → ✅ 34/34
- `_open_issues.md` empty? → ⚠️ M-005 (process), M-006 (deferred), L-005/L-006/L-007/L-008/L-009 (nit/deferred)

→ Cannot write `COMPLETE.md` yet. Awaiting code-004, design-003, iter-008.

## Conflicts / Communication

- **Push status:** Cycles 8 + code-003 doc + cycle 9 + cycle 10 commits all **local-only**. Awaiting credential rotation. Commits: d5764a5, e53dbd6, 297aafb, plus this one.
- **Iter-007 H-004 race:** The strict reviewer flagged uncommitted feature work — but cycle-9 commit `297aafb` landed before iter-007 finalized. The work was committed; the review's snapshot was stale. M-005 (commit-gate hook) is still legitimately open as a process concern.

## Notes for next cycle (cycle 11, mod 3 == 2 → Tests + polish)

- Add `ServiceRegistryViewModelTests` covering LoadPhase state machine
  (idle→loading→loaded; loading→error when allFailed; error→loading→loaded
  retry; legit-empty stays loaded not error) — locks in the cycle-9 + cycle-10
  contract per code-003 communication block.
- Address code-003 L1 (`StatusBadge` symbol+color presentation tuple) — pure
  cleanup if budget allows.
- Address strict-iter-007 M-006 properly: extend Domain `Service.createdAt`
  to optional or add `discoveredAt` companion field. Cross-module work but
  unlocks honest UI for launchd jobs.
