# Implementation Cycle 8 — 2026-04-20T00:10

**Module focus:** Tests + polish (cycle 8 mod 3 == 2)
**Time budget used:** ~10 / 30 min
**Reviews ingested this cycle:**
- `.review/code/code-002.md` ✅ (already consumed; addressing carried-over M3)
- `.design-review/design-002.md` ✅ (already consumed)
- `.review_strict/2026-04-19-2325-iter-004.md` ✅ (already consumed)
- `.review_strict/_open_issues.md` ✅ (only L-005 + L-006 OPEN, both nit)

No new reviews posted between cycle 7 and cycle 8 wakeup. Per `.review-prompts/implementer.md` §6, executing on the carried-over P0/P1 backlog rather than waiting.

## P1 fixes shipped (cycle 7's "next cycle" plan)

| ID | Stream | Fix |
|----|--------|-----|
| code-002 M3 (partial) | code-review | Extracted `StatusBadge` → `Sources/AgentJobsMac/Components/StatusBadge.swift`. |
| code-002 M3 (partial) | code-review | Extracted `MetricTile` → `Sources/AgentJobsMac/Components/MetricTile.swift`. |
| code-002 M2 (extension) | code-review | `ServiceRegistryTests` +2 cases: total-failure isolation (3 failing providers → empty, no throw) + 10-provider aggregation set-semantics test. Coverage on `ServiceRegistry.discoverAll()` now includes the worst-case path. |

`DashboardView.swift`: 335 → 282 LOC (−53). `MenuBarViews.swift` unchanged at 324 LOC (still has the largest extractables, deferred to cycle 11).

## Files touched (5)

- NEW `Sources/AgentJobsMac/Components/StatusBadge.swift` (~52 LOC)
- NEW `Sources/AgentJobsMac/Components/MetricTile.swift` (~25 LOC)
- `Sources/AgentJobsMac/Features/Dashboard/DashboardView.swift` — removed inline `StatusBadge` + `MetricTile` (53 LOC removed)
- `Tests/AgentJobsCoreTests/ServiceRegistryTests.swift` — +2 cases (~30 LOC)
- `CHANGELOG.md` — cycle 8 Added entry

## Test status

```
swift test → 27/27 passing in 0.302s  (was 25 last cycle)
- AgentJobsJsonProvider:    6
- CronHumanizer:            8
- MenuBarSummary:           3
- Shell:                    5
- ServiceRegistry:          5   ← +2 (total-failure, 10-provider aggregation)
```

Coverage trajectory: cycle 2 = 6 → 5 = 22 → 6 = 25 → 7 = 25 → 8 = 27 (+350% vs cycle 2).

## Strict-review CRITICAL/HIGH still open
**None.** Open register has only L-005, L-006 (LOW, marked OPEN-as-acceptable by iter-004 reviewer).

## Termination check progress

Per `.review-prompts/implementer.md` §8:

- Strict review iter-004 → ✅ PASS (2nd consecutive after iter-003)
- `_open_issues.md` empty? → ⚠️ 2 LOW items OPEN; iter-004 explicitly judged this acceptable for declare-done
- code-review-002 → 89/100 (CONTINUE — needs code-003 ≥ 90)
- design-review-002 → 82/100 (CONTINUE — needs design-003 ≥ 90)
- `swift test` green? → ✅ 27/27

→ Cannot write `COMPLETE.md` yet. Awaiting code-003, design-003, and iter-005. Cycle 8's component extraction + test additions should push code-003 over 90; cycle 7's UI polish should push design-003 over 90.

## Conflicts / Communication
None this cycle. Pure refactor (component extraction with no behavior change) + additive tests.

## Notes for next cycle (cycle 9, mod 3 == 0 → Discovery)

- Land `LaunchdUserProvider` skeleton via `Shell.run("/bin/launchctl", ["list"])` (architecture spec promises 4 sources; only `AgentJobsJsonProvider` ships today).
- Add a parser for the `launchctl list` 3-column output → `Service` records.
- Add a `LaunchdUserProviderTests` golden-file with a small captured fixture.
- Consider adding `Components/HoverableIconButton.swift` extraction from MenuBarViews.
