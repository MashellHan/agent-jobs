# Implementation Cycle 6 — 2026-04-19T23:40

**Module focus:** Discovery + tests (cycle 6 mod 3 == 0)
**Time budget used:** ~12 / 30 min
**Reviews ingested this cycle:**
- `.review/code/code-002.md` ✅ NEW (score 89/100, +31 vs 001)
- `.review_strict/2026-04-19-2255-iter-003.md` ✅ (PASS — already consumed cycle 5)
- `.review_strict/_open_issues.md` ✅ (still empty across all severities)
- `.design-review/design-001.md` ✅ (already consumed)

## P0 / P1 fixes shipped (code-002)

| ID | Stream | Fix |
|----|--------|-----|
| P0 #1 (M1) | code-002 | `Shell.Failure.timeout` — removed dead `partialStdout` param. Pattern is now `.timeout(seconds:)`. Updated `ShellTests` matcher accordingly. |
| P0 #2     | code-002 | `Shell.runProcess` cancellation — two-stage reap: SIGTERM, then SIGKILL after `sigtermGraceSeconds` (0.5s) if still running. Bounds worst-case hang at `timeout + 0.5s`. |
| P1 #3 (M2) | code-002 | New `ServiceRegistryTests` (3 cases) covering aggregation, failure-isolation, and empty-registry path. Closes the biggest test-coverage gap. |
| L2         | code-002 | `Shell.run` now `precondition`s on absolute path. |

## Deferred (will pick up next cycle)

- M3 (DashboardView 267 LOC extraction → `Components/StatusBadge`, `MetricTile`, etc.) — pure refactor; safer to do in dedicated UI cycle.
- M4 (Shell timeout-arm CancellationError log-noise under Swift 6 strict concurrency) — low impact; current behavior correct.
- L1 (`Shell.Result` shadows `Swift.Result`) — cosmetic.
- L4 (LICENSE file) — repo-level chore.
- L6 (`defaultRegistry()` only ships `AgentJobsJsonProvider`) — needs `LaunchdUserProvider` cycle.

## New tests

`ServiceRegistryTests` — 3 cases under `@Suite("ServiceRegistry orchestration")`:
1. **aggregates across providers** — two stub good providers each emit one service; result has both.
2. **failing provider does NOT poison the rest** — three providers (good + failing + slow); result has 2 services, failing one is logged + dropped.
3. **empty registry** — `discoverAll()` returns `[]` without crashing.

Three internal stub providers (`StubGoodProvider`, `StubFailingProvider`, `StubSlowProvider`) live in the test file — confined scope, no production impact.

## Files touched (5)

- `Sources/AgentJobsCore/Discovery/Shell.swift` — `Failure.timeout` shape change, `precondition` on path, two-stage SIGTERM→SIGKILL reap, expanded doc-comments.
- `Tests/AgentJobsCoreTests/ShellTests.swift` — updated `catch let Shell.Failure.timeout(seconds, _)` → `(seconds)`.
- NEW `Tests/AgentJobsCoreTests/ServiceRegistryTests.swift` (~95 LOC, 3 cases).
- `CHANGELOG.md` — cycle 6 Added/Changed entries.

## Test status

```
swift test → 25/25 passing in 0.313s  (was 22 last cycle)
- AgentJobsJsonProvider: 6
- CronHumanizer:         8
- MenuBarSummary:        3
- Shell:                 5
- ServiceRegistry:       3   ← NEW
```

Coverage trajectory: cycle 2 = 6 → cycle 3 = 17 → cycle 5 = 22 → cycle 6 = 25 (+316% vs cycle 2).

## Strict-review CRITICAL/HIGH still open
**None.** Open register remains empty across CRITICAL/HIGH/MEDIUM/LOW.

## Termination check progress

Per `.review-prompts/implementer.md` §8:

- Strict review iter-003 → ✅ PASS (no CRITICAL/HIGH); no iter-004 yet
- `_open_issues.md` empty? → ✅
- code-review-002 → 89/100 (CONTINUE — 1 short of 90 threshold)
- design-review-001 → 71/100 (CONTINUE; awaiting design-002)
- `swift test` green? → ✅ 25/25

→ Need code-003 ≥ 90 + design-002 ≥ 90 + iter-004 PASS for the **2nd** consecutive PASS round across all three streams. Cannot write `COMPLETE.md` yet.

## Conflicts / Communication
None this cycle. All changes are additive or strictly internal. The Shell `Failure.timeout` shape change is API-breaking for the public type — but no production caller uses the associated value yet (only `ShellTests` did, now updated).

## Notes for next cycle (cycle 7, mod 3 == 1 → UI layer)

- Address design-001 D-H2 (segmented Picker → chip row), D-H3 (refresh button hover), D-H5 (`.tableStyle(.inset(alternatesRowBackgrounds: true))`).
- Address code-002 M3: extract `StatusBadge`, `MetricTile`, `SidebarItem` from DashboardView.
- Possibly start `LaunchdUserProvider` skeleton (uses Shell.run("/bin/launchctl", ["list"])).
