# Implementation Cycle 11 — 2026-04-20T11:59

**Module focus:** Tests + polish (cycle 11 mod 3 == 2)
**Time budget used:** ~18 / 30 min
**Reviews ingested this cycle:**
- `.review_strict/_open_issues.md` ✅ (M-008 DiscoverResult coverage; M-006 synthetic createdAt — both top P1)
- `.review_strict/2026-04-20-0230-iter-010.md` ✅ (M-005 RESOLVED out-of-band by reviewer installing pre-commit-gate hook)

## P1 fixes shipped

| ID | Stream | Fix |
|----|--------|-----|
| strict M-008 | strict-review | `ServiceRegistryTests.swift` +4 tests for `DiscoverResult` contract: all-failing→allFailed=true, partial-success→allFailed=false, all-empty-success→allFailed=false (the M-007 false-positive scenario), empty-registry→allFailed=false. Locks in cycle-9/10 contract behaviorally. |
| strict M-006 | strict-review | `Service.createdAt` made optional `Date?` at the Domain layer. `LaunchdUserProvider` now passes `nil` (launchctl doesn't expose load time — was previously synthesizing `Date()` which lied about provenance). `DashboardView` "Created" column guards with `if let` and shows `—` for nil. `AgentJobsJsonProvider` still synthesizes via `createdAt ?? startedAt ?? Date()` so its data path is unchanged; only the type signature relaxed. |

## Files touched (6)

- `Sources/AgentJobsCore/Domain/Service.swift` — `createdAt: Date?` (was non-optional)
- `Sources/AgentJobsCore/Discovery/Providers/LaunchdUserProvider.swift` — `createdAt: nil`
- `Sources/AgentJobsMac/Features/Dashboard/DashboardView.swift` — `if let created = svc.createdAt { … } else { Text("—") }`
- `Tests/AgentJobsCoreTests/ServiceRegistryTests.swift` — +4 tests (~50 LOC)
- `Tests/AgentJobsCoreTests/AgentJobsJsonProviderTests.swift` — `.createdAt!` unwrap (provider always synthesizes non-nil)
- `.review_strict/_open_issues.md` — close M-008 + M-006

## Test status

```
swift test → 38/38 passing in 0.318s  (was 34, +4 from M-008 coverage)
```

## File-size watch

| File | LOC | Δ vs cycle 10 | Ceiling |
|------|-----|---------------|---------|
| `Service.swift` | 151 | +1 (Date? doc) | 400 |
| `ServiceRegistryTests.swift` | 175 | +50 | 600 |
| `DashboardView.swift` | 277 | +5 (if-let + else branch) | 400 |

## Termination check progress

- Strict review iter-010 → PASS-with-WARN; M-005 process gap RESOLVED by reviewer
- M-008 (top open P1) → CLOSED this cycle
- M-006 (4-cycle-old P1) → CLOSED this cycle
- `swift test` green? → ✅ 38/38
- `_open_issues.md` empty? → No — L-005..L-009 nits remain (deferred / cosmetic)

→ Cannot write `COMPLETE.md` yet. Awaiting iter-011 strict review confirming M-006/M-008 closure + 2nd consecutive ≥90 from code/design streams.

## Conflicts / Communication

- **Push status:** Cycles 7 + code-003 doc + cycle 9 + cycle 10 commits all **local-only** (4 ahead of origin/main). Awaiting credential rotation (`menha_microsoft` 403 vs `MashellHan/agent-jobs`). This cycle's commit will join the queue.
- **Pre-commit-gate hook:** Installed by iter-010 reviewer at `scripts/pre-commit-gate.sh` + `.claude/settings.json` PreToolUse hook. This commit will be the first to fire it. If it blocks the commit, will diagnose and fix the hook itself rather than bypass.

## Notes for next cycle (cycle 12, mod 3 == 0 → Discovery layer)

- Implement strict L-007/L-008: parse per-job plist files in `~/Library/LaunchAgents/` so launchd jobs surface real `.calendar`/`.interval` schedules instead of `.onDemand`.
- Consider `discoveredAt` companion field on `Service` to provide UI signal without lying about source-of-record (alternative to `createdAt = nil`).
- `MenuBarViews.swift` is at 120 LOC after cycle-10 extraction — comfortable headroom remains.
