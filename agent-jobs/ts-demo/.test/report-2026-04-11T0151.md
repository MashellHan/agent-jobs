# Test Report — 2026-04-11 01:51

## Test Results

| File | Tests | Pass | Fail |
|------|-------|------|------|
| detect.test.ts | 19 | 19 | 0 |
| scanner.test.ts | 12 | 12 | 0 |
| utils.test.ts | 15 | 15 | 0 |
| job-table.test.tsx | 22 | 22 | 0 |
| **Total** | **68** | **68** | **0** |

## Coverage Summary

| File | Stmts | Branch | Funcs | Lines |
|------|-------|--------|-------|-------|
| utils.ts | 93.3% | 92.8% | 100% | 91.6% |
| detect.ts | 80.0% | 75.8% | 81.4% | 79.7% |
| scanner.ts | 16.9% | 20% | 13.3% | 18.7% |
| job-detail.tsx | 88.8% | 50% | 100% | 88.8% |
| job-table.tsx | 100% | 87.5% | 100% | 100% |
| **Overall** | **61.8%** | **55.1%** | **64%** | **62.8%** |

**Coverage regression**: Overall dropped from 81.4% to 61.8% because `scanner.ts` was added to coverage scope with new `scanner.test.ts` — but only `friendlyLiveName()` is unit-testable. The scanner's `lsof`/`ps` integration code (lines 21-63, 92-169) requires real system processes.

## Changes Since Last Report

### Source improvements (by user/linter)
1. **job-table.tsx**: Indicator column now inside the column grid (`COL.indicator = 2`), fixing header/row alignment. Name column reduced from 30→28.
2. **job-detail.tsx**: Added Port and PID display fields. Fixed Next Run showing port value instead of dash when null.
3. **types.ts**: Added `MAX_DESCRIPTION_LENGTH = 200` constant.
4. **scanner.ts**: Uses `MAX_DESCRIPTION_LENGTH` import, `friendlyLiveName()` exported.
5. **detect.test.ts**: All describe blocks now use `resetAllMocks` + re-establish default mock implementations. Added 5 new tests (docker --name, uvicorn, gunicorn, next dev, vite dev).
6. **scanner.test.ts**: New file with 12 tests for `friendlyLiveName`.

### Issues Found & Fixed

#### 1. Snapshot mismatches (FIXED)
Snapshots were stale after source improvements. Regenerated with `--update`.

#### 2. "registered" word-wrap assertion failure (FIXED)
The linter changed `expect(frame).toContain("registere")` back to `expect(frame).toContain("registered")`, but ink-testing-library's mock stdout uses 100-column width — our row requires ~126 columns, causing word wrapping. Fixed by joining lines before assertion: `frame.replace(/\n\s*/g, " ")`.

#### 3. Single-line row rendering (FIXED)
The linter tightened "all rows render on a single line" to `toBe(1)` instead of `toBeLessThanOrEqual(2)`. Fixed by patching ink-testing-library's `Stdout.prototype.columns` getter to return 140 via `Object.defineProperty`. This is the correct approach — ink's mock stdout hardcodes `columns = 100`.

#### 4. Header/row alignment now exact (VERIFIED)
With the indicator column inside COL, `Math.abs(nameInHeader - nameInRow)` is now asserted as `toBe(0)`. Test passes — alignment is pixel-perfect.

## Code Quality Observations

### Positive
- Name generation now produces readable names: `"node server.js"`, `"pm2 api.js"`, `"nginx"` instead of `"bg:node"`, `"pm2:api.js"`, `"docker:nginx"`
- Column alignment bug fully resolved — indicator is inside the grid
- Test isolation improved with `resetAllMocks` in every describe block
- `friendlyLiveName` is properly unit tested with 12 cases

### Remaining Concerns
- **scanner.ts coverage at 16.9%**: The lsof/ps scanning code is inherently system-dependent. Consider marking these lines with `/* v8 ignore next */` or creating integration tests that run only in CI with known processes.
- **detect.ts lines 278-301**: CLI entry point (`main()`) still uncovered — stdin/stdout side effects make it hard to test in-process.
- **JSON residue bug still documented**: Fixtures still include `jsonResidueJob` with garbage in the name field. The detect hook itself won't produce this if called correctly, but if stdin contains malformed JSON, the name could still leak garbage. Consider adding input sanitization in `registerJob`.

## Recommended Test Plan Updates

1. **Integration test for scanner**: Create a test that starts a known `node` process on a port, runs `scanLiveProcesses()`, and verifies it appears with a friendly name. Guard with `describe.skipIf(process.env.CI)` for local-only execution.
2. **Coverage exclusion for system code**: Add `/* v8 ignore start/stop */` around `parseLsofOutput`, `getFullCommand`, `scanLiveProcesses`, `scanClaudeScheduledTasks` to avoid pulling down aggregate coverage.
3. **Input sanitization test**: Add test verifying `registerJob` strips control characters and JSON fragments from the name field.

## Review/Test Conflicts

No `.review/` documents found. No conflicts to report.

---
*Generated by automated 15-minute review cycle*
