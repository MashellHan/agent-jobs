// AgentJobsVisualHarness — Layer 2 of the visual harness pillar.
// Real modules land in T02..T07. This placeholder keeps the SPM
// target buildable until then.

import Foundation

public enum AgentJobsVisualHarness {
    public static let version = "0.1.0-m05"
}
