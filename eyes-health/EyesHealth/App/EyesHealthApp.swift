import SwiftUI

@main
struct EyesHealthApp: App {
    var body: some Scene {
        MenuBarExtra("EyesHealth", systemImage: "eye.fill") {
            Text("EyesHealth is loading...")
        }
    }
}
